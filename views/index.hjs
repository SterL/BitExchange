<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>
    </br>
    <h2>Going Rate For Various Cryptocurrencies From Various Exchanges In $USD</h2>

	<table border="1" style="width:100%">
  		<tr>
    		<th></th>
    		<th>BTC-E Exchange ($)</th> 
    		<th>Cryptsy Exchange ($)</th>
    		<th>Cryptonator ($)</th>
  		</tr>
  		<tr>
    		<th>BTC</th>
    		<td> ${{ ex.BTCEExchange.btc }}</td>
    		<td> ${{ ex.cryptsyExchange.btc }}</td>
    		<td> ${{ ex.cryptonatorExchange.btc }}</td>
  		</tr>
  		<tr>
  			<th>LTC</th>
  			<td> ${{ ex.BTCEExchange.ltc }} </td>
  			<td> ${{ ex.cryptsyExchange.ltc }} </td>
  			<td> ${{ ex.cryptonatorExchange.ltc }} </td>
  		</tr>
  		<tr>
  			<th>PPC</th>
  			<td> ${{ ex.BTCEExchange.ppc }} </td>
  			<td> ${{ ex.cryptsyExchange.ppc }} </td>
  			<td> ${{ ex.cryptonatorExchange.ppc }} </td>
  		</tr>
  		<tr>
  			<th>DASH</th>
  			<td> $ ? </td>
  			<td> ${{ ex.cryptsyExchange.dash}} </td>
  			<td> ${{ ex.cryptonatorExchange.dash }} </td>
  		</tr>
	</table>

	<p> (If one of the columns is not displayed, refresh the page) </p>

  </body>
</html>